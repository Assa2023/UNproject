Install the required libraries in both codebases (client and server) through "pnpm install"
Open app-dev.zip: It has client and server folders in it.
Then uncomment the bulk insertion code in index.js in the server codebase
Add the mongodb url connection as an environment variable in server folder
Start the application to connect with database and do the bulk insertion, then later comment the bulk insertion code. 
Then after the server is up and running, you can go on to start the client dev server and access the web app through localhost
